language: python

commands:
  build:
    - "pip install -r requirements.txt"

  package:
    - "nano .env"
    - "nano ShinobiCompass/main.py"
    - "cp .env ShinobiCompass/"
    - "zip -r ShinobiCompass.zip ShinobiCompass/"

  start:
    - "python3 ShinobiCompass/main.py"

env:
  - "ENVIRONMENT=production"
  - "DATABASE_URL=mongodb+srv://Akshayofficial:islandbot1904@tgbotproject.uwaxq.mongodb.net/?retryWrites=true&w=majority"
